#pragma once
#include "kswindows.h"

namespace ks
{
	enum class KeyCodes : char
	{
		A = 'A',
		D = 'D',
		S = 'S',
		W = 'W',
		Escape = VK_ESCAPE,
		Left = VK_LEFT,
		Right = VK_RIGHT,
		Up = VK_UP,
		Down = VK_DOWN,
		Return = VK_RETURN
	};
}